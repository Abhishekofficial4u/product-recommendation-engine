# ── Backend Configuration ────────────────────────────────────────────────────
# JWT secret key for signing tokens (change this to a secure random string in prod)
JWT_SECRET_KEY=rec_engine_super_secret_jwt_key_2026

# ── Frontend Configuration ───────────────────────────────────────────────────
# Base URL path pointing to the FastAPI backend REST endpoint
VITE_API_URL=http://localhost:8000
